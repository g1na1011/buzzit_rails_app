<h3>Make Friends!</h3>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user_path(user) %>
      [<%= link_to 'edit', edit_user_path(user) %>]
    </li>
  <% end %>
</ul>

